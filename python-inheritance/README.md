# Python - Inheritance

## Task 0: Lookup

### File: `0-lookup.py`

This file contains a Python function named `lookup` which returns a list of all attributes and methods available for an object using the built-in `dir()` function.

### Prototype
```python
def lookup(obj):
