# Python - Import & Modules

This project covers how to import and use functions from other Python files.

## Task 0: Import a simple function

**Files:**
- `add_0.py`: Contains the `add(a, b)` function
- `0-add.py`: Imports and uses the `add` function

### add_0.py

```python
def add(a, b):
    return (a + b)
